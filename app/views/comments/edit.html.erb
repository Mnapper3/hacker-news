<!--this is the form partial structure for adding a comment to a specific message in messgaes/show-->
<h1>Edit Comment</h1>
<%= simple_form_for([@message, @comment]) do |f| %>
  <%= f.input :content, label: "Comments", input_html: { class: "form-control" } %>
  <%= f.button :submit, class: "btn-primary" %>
<% end %>